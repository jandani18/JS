// example : 8

// loop from 1 to 10 using continue 

    for ( var guest=1; guest<=10; guest++)
    {
        if(guest==3)
        {
            continue;
        }
        console.log('guest',guest,'got the train')
    }

    // guest 1 got the train
    // guest 2 got the train
    // guest 4 got the train
    // guest 5 got the train
    // guest 6 got the train
    // guest 7 got the train
    // guest 8 got the train
    // guest 9 got the train
    // guest 10 got the train